// Create a mock logger
export const mockLogger = {
  debug: jest.fn(), // Mock the debug method
  info: jest.fn(),  // Mock the info method
  error: jest.fn(), // Mock the error method
  // Add other methods if needed
};